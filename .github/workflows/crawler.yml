name: Automated Wage Crawler

on:
  push:
    paths:
      - 'scripts/scraper/**'
  schedule:
    # Run at 18:00 UTC every Mon, Tue, Wed, Thu
    - cron: '0 18 * * 1,2,3,4'
  workflow_dispatch:
    inputs:
      target_state:
        description: 'State ID to crawl (e.g., dl, hr, up, ka) or "cron"'
        required: true
        default: 'cron'

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install -r scripts/scraper/requirements.txt

      - name: Run Crawler
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
          API_SECRET: ${{ secrets.API_SECRET }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SCRAPER_API_KEY: ${{ secrets.SCRAPER_API_KEY }}
          TARGET_STATE: ${{ github.event.inputs.target_state || 'cron' }}
        run: python -u scripts/scraper/crawler.py

      - name: Send Failure Notification via Resend
        if: failure()
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
        run: |
          curl -X POST https://api.resend.com/emails \
            -H "Authorization: Bearer $RESEND_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "from": "onboarding@resend.dev",
              "to": ["judge.raman@gmail.com", "raman@ramanjudge.com"],
              "subject": "🚨 Wage Crawler Failed!",
              "html": "<strong>Alert:</strong> The automated web crawler for Minimum Wages has failed during its recent run. Please check the GitHub Actions logs."
            }'
